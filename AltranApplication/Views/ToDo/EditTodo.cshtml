@model AltranApplication.Models.Todo

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit todo</title>
</head>
<body>
    <div>
        <h1>Edit todo : "@Model.Title"</h1>
        <hr />
        @using (Html.BeginForm("EditTodo", "ToDo", FormMethod.Post))
        {
            @Html.HiddenFor(m => m.TodoId)
            <h3>@Html.DisplayNameFor(model => model.Title) : </h3>
            @Html.TextBoxFor(m => m.Title)
            <hr />
            @*if (Model.State == true)
            {
                <input type="checkbox" checked /> <span>Done</span>
                <input type="checkbox" /> <span>Not Done</span>
            }
            else
            {
                <input type="checkbox"  /> <span>Done</span>
                <input type="checkbox" checked/> <span>Not Done</span>
            }*@
            <h3>@Html.DisplayNameFor(model => model.State) : </h3>
            @Html.CheckBoxFor(m => m.State) <span>Done</span>
            <hr />
            <h3>@Html.DisplayNameFor(model => model.Description) : </h3>
            @Html.TextAreaFor(m => m.Description)
            <br /><br />
            @* <button style="background-color:white">@Html.ActionLink("Valider", "UpdateTodo",new { Id = Model.TodoID})</button>*@
            <input type="submit" name="name" value="Save" />

        }
    </div>
</body>
</html>

